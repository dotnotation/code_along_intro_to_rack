source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}"}

gem 'rack'
gem 'pry'
gem 'shotgun'
#shotgun when run it will start the server and by default it will be looking for config.ru and it will run our call method
gem 'sinatra'